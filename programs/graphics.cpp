#include "graphics.h"
#include<iostream>
int main(){

    int gd=DETECT,gm,left=100,top=100,right=200,bottom=200,x=300,y=150,radius=50;
    circle(x,y,radius);
    getch();
    closegraph();
return 0;}
